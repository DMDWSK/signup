export const redirectToUpload = (props) => {
    props.history.push('/upload');
}

export const redirectToLogin = (props) => {
    props.history.push('/login');
}

export const redirectToRegister = (props) => {
    props.history.push('/register');
}
